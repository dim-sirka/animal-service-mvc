spring.freemarker.template-loader-path: classpath:/templates
spring.freemarker.suffix: .ftl

# ===============================
# DATABASE
# ===============================
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto: update
spring.jpa.show-sql: true
spring.jpa.generate-ddl: true
spring.datasource.url: ${DB_URL:jdbc:postgresql://ec2-79-125-86-58.eu-west-1.compute.amazonaws.com:5432/d7birrtpj6fr78;DB}
spring.datasource.username: ${DB_USERNAME:postgres}
spring.datasource.password: ${DB_PASSWORD:postgres}

# ===============================
# LOGGER
# ===============================
logging:
  level:
    root: INFO
    org.springframework.boot: INFO
    com.dimsirka.animalservice: INFO
    org.hibernate.stat: DEBUG

# ===============================
# Mail Config
# ===============================
mail.host: smtp.gmail.com
mail.port: 587
mail.username: ${EMAIL_USERNAME:email@gmail.com}
mail.password: ${EMAIL_PASSWORD:password}
mail.protocol: smtp
mail.smtp.starttls.enable: true
mail.debug: true
mail.smtp.auth: true
mail.adminEmail: ${ADMIN_EMAIL:admin@gmail.com}
